
class PassCracks {
    var id = "ks11030315"//データ取得
    var pass = "ks03151103"// データ取得
    var dict = sampledict.txt// 辞書データ参照
    def dictcracks() =

    def powernum() = //ブルートフォース

    def powerstr() = //ブルートフォース

    def pitagoras(x1,x2,y1,y2,z1,z2) = {
      for(a <- x1 to x2; b <- y1 to y2; c <- z1 to z2) {
        
      }
    }
}
